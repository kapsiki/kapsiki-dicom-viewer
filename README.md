# OHIF Frontend Development

- Create a local clone of  Kapsiki:OHIF/Viewers repository
- Install yarn using the command `npm install -g yarn`
- Enable yarn workspace with the command `yarn config set workspaces-experimental true`
- Install dependencies `yarn install`
- Run the development server `yarn run dev`

## Requirements

- NodeJS >=18
- Yarn
- Git
